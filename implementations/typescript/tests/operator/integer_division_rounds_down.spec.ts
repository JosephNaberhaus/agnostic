import { expect } from 'chai';

describe('TypeScript', () => {
  describe('Operator', () => {
    it('IntegerDivisionRoundsDown', () => {
      expect(Math.floor(11 / 3)).to.equal(3);
    });
  });
});
